import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<input ejs-colorpicker type='color' id='colorpicker'/>`
})
export class AppComponent {
  title = 'angular11ColorPicker-app';
}
